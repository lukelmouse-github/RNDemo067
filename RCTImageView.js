import { requireNativeComponent } from 'react-native';
/**
 * Composes `RCTImageView`.
 * 
 * - src: Image source (uri)
 * - borderRadius: number
 * - resizeMode: 'cover' | 'contain' | 'stretch'
 */
export default requireNativeComponent('RCTImageViewDemo');
